<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description %>
    <%= f.button :submit, "Add the dose", class: "btn btn-success"%>
  <% end %>


  <p><%= link_to "<= back", root_path %></p>
</div>
