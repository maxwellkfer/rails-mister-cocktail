<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.button :submit, "Add the dose"%>
<% end %>


<p><%= link_to "<= back", root_path %></p>
